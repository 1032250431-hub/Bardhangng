/* React Bits GlassSurface adaptation for MED-ROUTE's existing HTML/JS architecture. */
(()=>{const mount=()=>{document.querySelectorAll('.panel,.panel-block,.command-wrap,.hero-side,.topbar').forEach((el)=>{if(el.dataset.glassSurface==='1')return;el.dataset.glassSurface='1';el.classList.add('mr-glass-surface');const content=document.createElement('div');content.className='mr-glass-content';while(el.firstChild)content.appendChild(el.firstChild);el.appendChild(content);const radius=getComputedStyle(el).borderRadius;el.style.setProperty('--glass-radius',radius==='0px'?'20px':radius)})};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount()})();