/* Rural Healthcare Routing Engine
 * Authoritative browser-compatible engine.
 * Weighted graph + binary min heap + Dijkstra + resource-aware dispatch.
 */
const SPECIALTIES=["Cardiology","Trauma","Neurology","Pediatrics"];
const NODE_COUNT=500;
const HOSPITAL_COUNT=5;
const AMBULANCE_COUNT=10;
const LOW_BED_THRESHOLD=2;
const LOW_BED_WAIT_PENALTY=15;
class MinPriorityQueue{constructor(){this.heap=[]}isEmpty(){return this.heap.length===0}size(){return this.heap.length}enqueue(value,priority){this.heap.push({value,priority});let i=this.heap.length-1;while(i>0){const p=Math.floor((i-1)/2);if(this.heap[p].priority<=this.heap[i].priority)break;[this.heap[p],this.heap[i]]=[this.heap[i],this.heap[p]];i=p}}dequeue(){if(!this.heap.length)return null;if(this.heap.length===1)return this.heap.pop();const min=this.heap[0];this.heap[0]=this.heap.pop();let i=0;while(true){const l=i*2+1,r=i*2+2;let s=i;if(l<this.heap.length&&this.heap[l].priority<this.heap[s].priority)s=l;if(r<this.heap.length&&this.heap[r].priority<this.heap[s].priority)s=r;if(s===i)break;[this.heap[i],this.heap[s]]=[this.heap[s],this.heap[i]];i=s}return min}}
const randomInt=(a,b)=>Math.floor(Math.random()*(b-a+1))+a;
const randomFloat=(a,b)=>Math.random()*(b-a)+a;
const randomItem=a=>a[Math.floor(Math.random()*a.length)];
function shuffle(a){const r=[...a];for(let i=r.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[r[i],r[j]]=[r[j],r[i]]}return r}
function generateMockGraph(){const graph=new Map(),nodes=[];for(let i=0;i<NODE_COUNT;i++){const n={id:`N-${i+1}`,coordinates:[randomFloat(18.8,19.2),randomFloat(72.7,73.3)],type:i<50?'VILLAGE':'INTERSECTION'};nodes.push(n);graph.set(n.id,[])}const add=(a,b,t)=>{graph.get(a).push({to:b,travelTime:t,blocked:false});graph.get(b).push({to:a,travelTime:t,blocked:false})};for(let i=1;i<nodes.length;i++)add(nodes[i].id,nodes[randomInt(0,i-1)].id,randomInt(3,30));for(let i=0;i<NODE_COUNT*2;i++){const a=randomItem(nodes),b=randomItem(nodes);if(a.id!==b.id)add(a.id,b.id,randomInt(3,30))}const hospitals=[];for(const [i,n] of shuffle(nodes).slice(0,HOSPITAL_COUNT).entries()){n.type='HOSPITAL';hospitals.push({id:`H-${i+1}`,nodeId:n.id,coordinates:[...n.coordinates],specialty:shuffle(SPECIALTIES).slice(0,randomInt(1,4)),beds:randomInt(5,20),medicine:randomInt(10,100)})}const ambulances=[];for(let i=0;i<AMBULANCE_COUNT;i++)ambulances.push({id:`AMB-${String(i+1).padStart(2,'0')}`,currentNode:randomItem(nodes).id,status:'IDLE'});return{graph,nodes,hospitals,ambulances}}
function runDijkstra(start,state,respectClosures=true){const d=new Map(),p=new Map();state.nodes.forEach(n=>d.set(n.id,Infinity));d.set(start,0);const q=new MinPriorityQueue();q.enqueue(start,0);while(!q.isEmpty()){const {value:u,priority:du}=q.dequeue();if(du!==d.get(u))continue;for(const e of state.graph.get(u)||[]){if(respectClosures&&e.blocked)continue;const nd=du+e.travelTime;if(nd<d.get(e.to)){d.set(e.to,nd);p.set(e.to,u);q.enqueue(e.to,nd)}}}return{distances:d,previous:p}}
function reconstructPath(start,end,previous){const r=[];for(let x=end;x!==undefined;x=previous.get(x)){r.push(x);if(x===start)break}if(r[r.length-1]!==start)return null;return r.reverse()}
function evaluateHospital(h,specialty){return{hasSpecialty:h.specialty.includes(specialty),hasBeds:h.beds>0,hasMedicine:h.medicine>0}}
function findOptimalRoute(startNode,requiredSpecialty,state,respectClosures=true){if(!state.graph.has(startNode))return{success:false,reason:'START_NODE_NOT_FOUND'};if(!SPECIALTIES.includes(requiredSpecialty))return{success:false,reason:'INVALID_SPECIALTY'};const {distances,previous}=runDijkstra(startNode,state,respectClosures);let best=null,travel=Infinity,cost=Infinity,wait=0;for(const h of state.hospitals){const d=distances.get(h.nodeId);const e=evaluateHospital(h,requiredSpecialty);if(!e.hasSpecialty||!e.hasBeds||!e.hasMedicine||d===Infinity)continue;const w=h.beds<=LOW_BED_THRESHOLD?LOW_BED_WAIT_PENALTY:0,c=d+w;if(c<cost){best=h;travel=d;wait=w;cost=c}}if(!best)return{success:false,reason:'NO_FEASIBLE_HOSPITAL',requiredSpecialty};const route=reconstructPath(startNode,best.nodeId,previous);if(!route)return{success:false,reason:'PATH_RECONSTRUCTION_FAILED'};return{success:true,startNode,requiredSpecialty,hospital:best,path:route,travelTime:travel,waitPenalty:wait,compositeCost:cost,algorithm:'Dijkstra + Binary Min Heap',complexity:'O((V + E) log V)'}}
function findNearestAvailableAmbulance(startNode,state,distances){const idle=state.ambulances.filter(a=>a.status==='IDLE');if(!idle.length)return{success:false,reason:'NO_AVAILABLE_AMBULANCE'};const d=distances||runDijkstra(startNode,state,true).distances;let best=null,bestDistance=Infinity;for(const a of idle){const x=d.get(a.currentNode);if(x!==undefined&&x<bestDistance){best=a;bestDistance=x}}return best?{success:true,ambulance:best,distance:bestDistance}:{success:false,reason:'NO_REACHABLE_AMBULANCE'}}
function setRoadBlocked(state,fromNode,toNode,blocked=true){let changed=false;for(const e of state.graph.get(fromNode)||[]){if(e.to===toNode){e.blocked=blocked;changed=true}}for(const e of state.graph.get(toNode)||[]){if(e.to===fromNode)e.blocked=blocked}return changed}
function processEmergency(startNode,requiredSpecialty,state){const decisionLog=[`Emergency received at ${startNode}. Required specialty: ${requiredSpecialty}.`];if(!state.graph.has(startNode))return{success:false,status:'INVALID_REQUEST',reason:'START_NODE_NOT_FOUND',decisionLog};if(!SPECIALTIES.includes(requiredSpecialty))return{success:false,status:'INVALID_REQUEST',reason:'INVALID_SPECIALTY',decisionLog};const idle=state.ambulances.filter(a=>a.status==='IDLE');if(!idle.length){decisionLog.push('All local ambulances currently occupied.');return{success:false,status:'QUEUED_NO_AMBULANCE',reason:'All local ambulances currently occupied.',decisionLog}}const {distances,previous}=runDijkstra(startNode,state,true);let nearest=null,nearestDistance=Infinity,best=null,bestTravel=Infinity,bestCost=Infinity,bestWait=0;for(const h of state.hospitals){const d=distances.get(h.nodeId);if(d!==undefined&&d<nearestDistance){nearest=h;nearestDistance=d}const e=evaluateHospital(h,requiredSpecialty);if(!e.hasSpecialty){decisionLog.push(`Hospital ${h.id} rejected: lacks ${requiredSpecialty}.`);continue}if(!e.hasBeds){decisionLog.push(`Hospital ${h.id} rejected: 0 beds available.`);continue}if(!e.hasMedicine){decisionLog.push(`Hospital ${h.id} rejected: 0 medicine available.`);continue}if(d===Infinity){decisionLog.push(`Hospital ${h.id} has no direct available road route. Skipping.`);continue}const w=h.beds<=LOW_BED_THRESHOLD?LOW_BED_WAIT_PENALTY:0,c=d+w;if(c<bestCost){best=h;bestTravel=d;bestWait=w;bestCost=c}}if(nearest){const e=evaluateHospital(nearest,requiredSpecialty);if(!e.hasSpecialty||!e.hasBeds||!e.hasMedicine)decisionLog.unshift(`Hospital ${nearest.id} is closer (${nearestDistance}) but is infeasible for this request. Rerouting...`)}if(!best)return{success:false,status:'NO_FEASIBLE_HOSPITAL',reason:'No hospital has the required specialty and available bed and medicine resources.',requiredSpecialty,decisionLog};const route=reconstructPath(startNode,best.nodeId,previous);if(!route)return{success:false,status:'PATH_RECONSTRUCTION_FAILED',reason:'Selected hospital route could not be reconstructed.',decisionLog};const ar=findNearestAvailableAmbulance(startNode,state,distances);if(!ar.success)return{success:false,status:'QUEUED_NO_AMBULANCE',reason:'All local ambulances currently occupied.',decisionLog};if(best.beds<=0||best.medicine<=0)return{success:false,status:'RESOURCE_DEPLETED',reason:'Hospital resources are no longer sufficient.',decisionLog};best.beds-=1;best.medicine-=1;ar.ambulance.status='DISPATCHED';decisionLog.push(`Routed to Hospital ${best.id} (Distance: ${bestTravel}). Dispatched Ambulance ${ar.ambulance.id}. Hospital ${best.id} beds remaining: ${best.beds}.`);decisionLog.push(`Hospital ${best.id} medicine remaining: ${best.medicine}.`);if(bestWait)decisionLog.push(`Low-bed wait penalty ${bestWait} applied.`);decisionLog.push(`Final composite routing cost: ${bestCost}.`);return{success:true,status:'DISPATCHED',startNode,requiredSpecialty,hospital:best,ambulance:ar.ambulance,path:route,travelTime:bestTravel,waitPenalty:bestWait,compositeCost:bestCost,ambulanceDispatchDistance:ar.distance,decisionLog,algorithm:'Dijkstra + Binary Min Heap + Resource-Aware Dispatch',complexity:'O((V + E) log V)',stateMutation:{hospitalBedsRemaining:best.beds,hospitalMedicineRemaining:best.medicine,ambulanceStatus:ar.ambulance.status}}}
function createHealthcareEngine(){const state=generateMockGraph();return{...state,findOptimalRoute:(n,s)=>findOptimalRoute(n,s,state,true),findOptimalRouteIgnoringClosures:(n,s)=>findOptimalRoute(n,s,state,false),processEmergency:(n,s)=>processEmergency(n,s,state),findNearestAvailableAmbulance:(n,d)=>findNearestAvailableAmbulance(n,state,d),setRoadBlocked:(a,b,c=true)=>setRoadBlocked(state,a,b,c)}}
if(typeof window!=='undefined')window.createHealthcareEngine=createHealthcareEngine;
if(typeof module!=='undefined'&&module.exports)module.exports={SPECIALTIES,MinPriorityQueue,generateMockGraph,findOptimalRoute,findNearestAvailableAmbulance,setRoadBlocked,processEmergency,createHealthcareEngine};
